# airflow_nomad.load_airflow_config

### airflow_nomad.load_airflow_config(config_dir: str | Path, config_name: str, , overrides: list[str] | None = None, \*\*values: Any) → NomadConfiguration

Compose a Hydra YAML configuration and validate it.
